// tree class & build tree function
class Tree {
    // constructor
    constructor(arr) {
        this.arr = arr;
        // this.root = buidTree(sortedArr, 0, sortedArr.length - 1)        
    };
};